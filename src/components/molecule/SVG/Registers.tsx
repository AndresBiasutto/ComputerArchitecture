import {registers} from "../../../assets/ImageLinks";
const Registers = () => {
  return (
    <div className="w-full h-full">
      <img className="w-full" src={registers} alt="" />
    </div>
  );
};

export default Registers;