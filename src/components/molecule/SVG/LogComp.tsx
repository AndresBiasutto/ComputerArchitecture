import {logicComponents} from "../../../assets/ImageLinks"

const LogComp = () => {
  return (
    <div className="w-full h-full">
      <img className="w-full" src={logicComponents} alt="" />
    </div>
  );
};

export default LogComp;