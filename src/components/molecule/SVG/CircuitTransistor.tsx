import {chipsetAndIntegratedCircuit} from "../../../assets/ImageLinks"

const CircuitTransistor = () => {
  return (
    <div className="w-full h-full">
      <img className="w-full" src={chipsetAndIntegratedCircuit} alt="" />
    </div>
  );
};

export default CircuitTransistor;
